# Hoodi RFQ deployment — 2026-09-07

The solver reads operational addresses from YAML; no Hoodi addresses are hardcoded in Go.
The companion `vault-solver-deploy` repository wires the following defaults on chain **560048**,
verified from deployment receipts, code and getters at block **3578851**:

| Contract / workload | Address |
| --- | --- |
| `HOODI_RFQ_REACTOR` | `0xe8a937d1324ff5a09a04eca610d8f7936c9d68a6` |
| `HOODI_RFQ_EXECUTOR` | `0xfbafa115096c2cf5406de169420484d49f56ebce` |
| `HOODI_RFQ_EXECUTOR_FIRST` | `0x310956571ce0e045ba7d7f04f31ddbb95cbf179e` |
| `HOODI_RFQ_EXECUTOR_SECOND` | `0xeec9be69661b6072c341389aa6e64919060833ea` |
| `HOODI_RFQ_EXECUTOR_THIRD` | `0x5c6801c5d96e8929ec955ea8037f55bd2e338c89` |

The Reactor's `LIQUID_LANE_ADAPTER_FACTORY()` is
`0x0360F649A90F2Cd98c87E499a1aca230F005C33d`. Each Executor's owner and authorized caller
were verified; operators still need to match the actual workload signer to that caller.

Hoodi rollout stays disabled. The USDC adapter `0xBB1dDb29a50B365824cf6C20874adDE6A58fdD86`
and aUSD adapter `0xB00Bf2E3767C5711389231db1D72D257eaF7d2e7` still report core executor
`0x7C8132ACc1Ef9f9E9B01C2136F42b29c6254B792` as owner/marketMaker. The new RFQ Executors
have no verified direct adapter permissions. External mode intentionally fails startup without
direct authorization; internal signed-discount execution is possible but remains unproven.
Numbered workload adapter assignments are unspecified and must not be inferred from the two adapters.
Backend routing, token classification, and an executable fill path still need verification.

CuratorRegistry, FeeRegistry, Rewards and BurnerRouterFactory are not runtime solver config fields.
The operational service protocol signer is not a contract deployment input. There is no Hoodi 3F
deployment and no 3F prerequisite for these RFQ workloads. The mainnet example YAML remains mainnet.
No solver deployment, live signature or fill was performed by this configuration update.
